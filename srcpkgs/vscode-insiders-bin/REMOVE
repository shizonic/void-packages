if [ "$UPDATE" = "no" ]; then
    case "$ACTION" in
        post)
            if [ -d usr/share/code-insiders ]; then
                rm -rf usr/share/code-insiders
            fi
        ;;
    esac
fi
